{
	"name": "lectern-scripts",
	"private": true,
	"description": "Scripts for managing the Lectern monorepo.",
	"scripts": {
		"generate": "pnpm run generate:meta && pnpm run generate:samples",
		"generate:meta": "node -r ts-node/register ./src/generateMetaSchema.ts",
		"generate:samples": "node -r ts-node/register ./src/generateSampleDictionaries.ts"
	},
	"keywords": [],
	"license": "AGPL-3.0",
	"dependencies": {
		"@overture-stack/lectern-dictionary": "workspace:^",
		"zod": "^3.23.8",
		"zod-to-json-schema": "^3.23.2"
	}
}
