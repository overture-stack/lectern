export const scriptWithNewlines =
	"(function validate(inputs) {\r\nconst {$row, $name, $field} = inputs;\r\nreturn {valid: true, message: 'Ok';\r\n}";
export const scriptWithNewlinesNormalized =
	"(function validate(inputs) {\nconst {$row, $name, $field} = inputs;\nreturn {valid: true, message: 'Ok';\n}";
